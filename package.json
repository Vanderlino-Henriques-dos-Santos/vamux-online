{
  "name": "vamux",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": { // <<-- Adicione esta seção
    "node": "18" // <<-- Com a versão do Node.js que deseja usar
  },             // <<-- Certifique-se de que a linha "type": "module", termina com vírgula!
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}